# Method Compatibility Matrix

This document is the method-level contract for backend compatibility, routing
mode, fallback behavior, and semantic risk. It is generated from
[capabilities.yaml](capabilities.yaml).

It focuses on routed, downgraded, bridged, or risk-bearing RPC families rather
than every overlapping Apache HMS method. Front-door-local security token APIs
remain documented in [README.md](README.md).

Status legend:

- `supported`: normal path for that backend/runtime combination
- `degraded`: works through a downgrade, default-catalog pin, or compatibility response
- `rejected`: fails explicitly instead of guessing
- `unsafe`: can route, but multi-catalog semantics are not production-safe until validated

Refresh the generated table with:

```bash
mvn -o -q -Dtest=CapabilityMatrixDocSyncTest -Dcapabilities.updateReadme=true test
```

<!-- BEGIN GENERATED: method-compatibility-matrix -->
| Method(s) | Apache backend | HDP backend | Hive 4 backend | Frontend profile support | Routing mode | Fallback strategy | Semantic risk flag |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Direct object RPCs such as `get_database`, `get_table`, `create_table`, `alter_table`, `drop_table`, `drop_function` | `supported`: Native Apache HMS handling once one catalog is resolved. | `supported`: Same routing model for overlapping Apache RPCs on Hortonworks backends. | `supported`: Same routing model; positional `get_table` and `get_table_objects_by_name` (removed in Hive 4) are auto-upgraded to their `*_req` equivalents by the Hive 4 backend adapter, everything else flows through binary-compatible Thrift delegation. | `APACHE_3_1_3`; overlapping RPCs also work through `HORTONWORKS_*` front doors | Prefer explicit proxy `catName`, then externalized `dbName` / `fullTableName`, else `routing.default-catalog` | None. | Low, as long as the request resolves one catalog unambiguously. |
| List/discovery reads such as `get_all_databases`, `get_databases`, `get_all_tables`, `get_tables`, `get_table_meta` | `supported`: Fanout/list aggregation works across Apache backends. | `supported`: Same fanout/list aggregation model across Hortonworks backends. | `supported`: Same fanout/list aggregation model across Hive 4 backends. | Any front-door profile that exposes the overlapping RPCs. | Fan out across eligible catalogs when no namespace is present; route to one backend when `dbName` or `fullTableName` resolves a catalog | None beyond namespace externalization and optional selective exposure filtering. | Low; list results can be filtered and merged across catalogs, so expect externalized names instead of raw backend names |
| `get_database_req`, `get_table_req`, `get_table_objects_by_name_req` | `supported`: Native Apache request-wrapper handling. | `degraded`: Retries legacy `get_database`, `get_table`, or `get_table_objects_by_name` when the wrapper API is missing | `supported`: Native Hive 4 request-wrapper handling; `*_req` methods are the canonical Hive 4 shape and reach the backend through binary-compatible delegation. | `APACHE_3_1_3` | Same namespace routing as the equivalent direct object RPCs. | Backend adapter downgrades to the legacy method only on `TApplicationException UNKNOWN_METHOD` (or a missing method in the loaded runtime) and caches that wrapper as unsupported; other backend errors are propagated and never cached | Low; metadata semantics stay aligned, but the call path becomes legacy. |
| `getMetaConf`, `get_all_functions`, `get_metastore_db_uuid` | `degraded`: Served from `routing.default-catalog` only. | `degraded`: Served from `routing.default-catalog` only. | `degraded`: Served from `routing.default-catalog` only. | Any front-door profile that exposes the overlapping RPCs. | Default-catalog pin because the RPC has no catalog namespace. | None. | Medium; callers only see one catalog's global service state. |
| `get_current_notificationEventId`, `get_next_notification`, `get_notification_events_count`, `get_open_txns`, `get_open_txns_info`, `show_locks`, `show_compact` | `degraded`: Default-catalog scoped; an empty compatibility response is emitted only when the backend does not implement the method. | `degraded`: Default-catalog scoped; an empty compatibility response is emitted only when the backend does not implement the method. | `degraded`: Default-catalog scoped; an empty compatibility response is emitted only when the backend does not implement the method. | Any front-door profile that exposes the overlapping RPCs. | Default-catalog pin because the RPC has no catalog namespace. | Return an empty or synthetic compatibility response only on `TApplicationException UNKNOWN_METHOD` (or a missing method in the loaded runtime); transport failures, `MetaException`, and other `TApplicationException` types are propagated to the caller | Medium; an empty answer is a statement about ACID, lock, and notification state, so it is limited to backends that genuinely lack the RPC, while transient failures stay visible as errors |
| `get_privilege_set`, `list_privileges`, `get_role_names`, `get_principals_in_role`, `get_role_grants_for_principal`, `refresh_privileges`, `get_all_token_identifiers`, `get_master_keys` | `degraded`: Empty or success-shaped compatibility response only when the backend does not implement the method. | `degraded`: Empty or success-shaped compatibility response only when the backend does not implement the method. | `degraded`: Empty or success-shaped compatibility response only when the backend does not implement the method. | Any front-door profile that exposes the overlapping RPCs. | Normal namespace routing, or default-catalog pin when the RPC has no namespace. | Return an empty or success-shaped compatibility response only on `TApplicationException UNKNOWN_METHOD` (or a missing method in the loaded runtime); a failed authorization call is never reported as an empty privilege set or a successful refresh | Medium; an empty privilege answer can be read as an authorization decision, so it is limited to backends that genuinely lack the RPC |
| `get_active_resource_plan`, `get_all_resource_plans`, `get_runtime_stats` | `degraded`: Default-catalog scoped and may emit compatibility empty responses. | `degraded`: Default-catalog scoped and may emit compatibility empty responses. | `degraded`: Default-catalog scoped and may emit compatibility empty responses. | Any front-door profile that exposes the overlapping RPCs. | Default-catalog pin because the RPC has no catalog namespace. | Return an empty compatibility response on any backend failure (`TApplicationException`, transport failure, or `MetaException`), because these RPCs only expose optional workload-management and diagnostic state | Low; an empty answer hides diagnostics, but it does not change data, lock, or authorization semantics |
| `open_txns`, `commit_txn`, `abort_txn`, `abort_txns`, `check_lock`, `unlock`, `heartbeat`, `heartbeat_txn_range` | `degraded`: Pinned to `routing.default-catalog`; synthetic lock shim only helps non-ACID `SELECT` and eligible `NO_TXN` DDL locks on non-default catalogs | `degraded`: Same default-catalog control-plane constraint on Hortonworks backends. | `degraded`: Same default-catalog control-plane constraint on Hive 4 backends; Hive 4 ACID v2 extensions are not exposed by this proxy and stay out of scope. | Any front-door profile. | Default-catalog pin once only ids remain in the payload. | Synthetic read-lock shim for eligible non-default-catalog lock cases, otherwise the normal default-backend txn/lock lifecycle | High; treat this as a single-catalog control plane unless you validated the exact workload end to end |
| `get_valid_write_ids`, `allocate_table_write_ids`, `compact`, `compact2`, `add_dynamic_partitions`, `fire_listener_event`, `repl_tbl_writeid_state` | `unsafe`: Routed by payload namespace when one can be extracted. | `unsafe`: Same payload-based routing, but backend/runtime semantics still need validation. | `unsafe`: Same payload-based routing; Hive 4 has additional ACID v2 lifecycle RPCs that this proxy does not expose, so multi-catalog ACID semantics still need validation per workload. | Any front-door profile that exposes the RPC. | Extract `dbName` or `fullTableName` from the payload; if no object namespace survives, fall back to the default-catalog lifecycle rules | No semantic downgrade beyond payload extraction. | High; ACID and write-id semantics are not guaranteed safe across catalogs, so treat these RPCs as production-unsafe until validated |
| `get_database_req`, `truncate_table_req`, `alter_table_req`, `alter_partitions_req`, `rename_partition_req`, `get_partitions_by_names_req`, `update_table_column_statistics_req`, `update_partition_column_statistics_req` | `supported`: Frontend bridge adapts the HDP wrapper to Apache handler equivalents. | `supported`: Same bridge on the front door, then normal routing to the resolved backend. | `supported`: Same bridge on the front door produces an Apache-shaped request that flows through binary-compatible delegation to the Hive 4 backend. | `HORTONWORKS_*` with standalone jar | Follows the bridged Apache equivalent for namespace and default-catalog policy. | Frontend bridge rewrites the request and, when needed, the response type. | Low for request shapes that have a safe Apache mapping. |
| `create_table_req` | `degraded`: Bridged to `create_table`, `create_table_with_environment_context`, or `create_table_with_constraints`; rejected when one request mixes constraints with `envContext` | `degraded`: Same bridge behavior before routing to the resolved backend. | `degraded`: Same bridge behavior before routing the resulting Apache-shaped call to the Hive 4 backend. | `HORTONWORKS_*` with standalone jar | Namespace is taken from the table object after bridge conversion. | Frontend bridge picks the safest Apache equivalent and rejects request shapes without a safe mapping | Medium; ordinary shapes are fine, but mixed constraint + envContext requests fail explicitly. |
| `add_write_notification_log` | `rejected`: Apache backends do not provide a compatible Hortonworks extension runtime. | `supported`: Direct Hortonworks passthrough to the resolved Hortonworks backend. | `rejected`: Hive 4 backends do not provide the Hortonworks extension runtime. | `HORTONWORKS_*` with standalone jar | Explicit namespace routing to one resolved catalog/backend. | None. | Low when the target backend runtime is explicitly Hortonworks. |
| `get_tables_ext` | `rejected`: Apache backends do not implement this Hortonworks-only RPC. | `supported`: Direct Hortonworks passthrough only to `HORTONWORKS_3_1_0_3_1_5_6150_1` | `rejected`: Hive 4 backends do not implement this Hortonworks-only RPC. | `HORTONWORKS_*` with standalone jar | Explicit namespace routing to one resolved catalog/backend. | None. | Low; the main risk is runtime mismatch, which is rejected explicitly. |
| `get_all_materialized_view_objects_for_rewriting` | `rejected`: Apache backends do not implement this Hortonworks-only RPC. | `supported`: Direct Hortonworks passthrough only to `HORTONWORKS_3_1_0_3_1_5_6150_1` | `rejected`: Hive 4 backends do not implement this Hortonworks-only RPC. | `HORTONWORKS_*` with standalone jar | Pinned to `routing.default-catalog`. | None. | Medium; the call is default-catalog scoped and materialized-view rewrite semantics remain runtime-specific |
| `setMetaConf`, `grant_role`, `revoke_role`, and other namespace-less service writes | `rejected`: Safely failed when the request does not resolve exactly one catalog under the routing policy. | `rejected`: Safely failed when the request does not resolve exactly one catalog under the routing policy. | `rejected`: Safely failed when the request does not resolve exactly one catalog under the routing policy. | Any front-door profile. | No guessed routing; the mutation must resolve one owned namespace or fail safely up front | None. | Blocked by design to guarantee deterministic routing and prevent silent split-brain writes or drifting global state. |
| `create_catalog`, `alter_catalog`, `drop_catalog` | `rejected`: Catalog registry is policy-owned by config, not runtime-managed through client RPCs. | `rejected`: Catalog registry is policy-owned by config, not runtime-managed through client RPCs. | `rejected`: Catalog registry is policy-owned by config, not runtime-managed through client RPCs. | Any front-door profile. | No routing; catalog registry changes are never proxied. | None. | Blocked by design so namespace ownership stays explicit and centrally governed. |
<!-- END GENERATED: method-compatibility-matrix -->
