# Overrides that turn the shared hadoop.env into the *second* HDFS cluster.
#
# Listed after hadoop.env in the service's env_file, so fs.defaultFS points at this cluster's own
# namenode. Everything else (replication, hostname resolution, proxyuser rules) is shared.
CORE_CONF_fs_defaultFS=hdfs://namenode-b:8020
