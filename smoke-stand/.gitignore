# Build inputs staged by prepare.sh — large, and reproducible from the repo/Maven.
hms/lib/
hms/*.jar
hs2/lib/
proxy/hive-metastore/
proxy/hms-proxy-fat.jar

# Maven output of the classpath-resolving poms.
*/target/

# Vendor HDP runtime staged by prepare.sh from HDP_DIST_DIR. Not redistributable.
hs2-hdp/dist/
hms/acid-apache/
hms/acid-hdp/
